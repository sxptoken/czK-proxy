# czX Search

Upload these files to the ROOT of a GitHub repository, then import that repository into Vercel.

The API uses CommonJS (`module.exports`) for reliable Vercel serverless deployment.

Structure:
- index.html
- style.css
- package.json
- vercel.json
- api/search.js

No environment variables or API keys are required.
